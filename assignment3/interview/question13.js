// the purpose of the useContext() hook in React
//The useContext hook allows you to use the context API to share state across components without passing props manually
const value = useContext(MyContext);
